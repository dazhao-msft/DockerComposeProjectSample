version: '2'

services:
  webapplication1:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "80"

  webapplication2:
    environment:
      - ASPNETCORE_ENVIRONMENT=Development
    ports:
      - "80"
